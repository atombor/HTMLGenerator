package htmlbuilder.component;

/**
 * Interface that defines that the object is an HTML element.
 * 
 * @author Attila Tombor
 */
public interface HtmlComponent {
	
	/**
	 * Prints the HTML content of the HTML element.
	 * 
	 * @return
	 */
	String print();
	
}
